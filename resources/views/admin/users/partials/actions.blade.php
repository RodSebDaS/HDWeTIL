<div>
    <x-actions rutaEdit="{{ route('admin.users.edit', $id) }}" puedeEditar="admin.users.edit"
        rutaDestroy="{{ route('admin.users.destroy', $id) }}" puedeEliminar="admin.users.destroy" />
</div>
